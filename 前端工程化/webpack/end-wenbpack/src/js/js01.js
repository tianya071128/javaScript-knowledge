console.log(22);

/**
 * @description: 测试函数
 * @param {Number} a
 * @param {Number} b
 * @param {Number} c
 * @param {Number} d
 * @return {Number}
 */
export default function test(a, b, c, d) {
  return a + b + c + d;
}